# Roadmap — classification/ (Phase 4: Anemia Classification)

High-level, sequential plan for this module only. `[x]` = done and verified (not just written). This directory is intentionally decoupled from the root project's Phase 0-3 segmentation work — see `03_tech_stack_and_rules.md` for the isolation rules.

## Data preparation
- [x] Extract `archive.zip` fresh into `data/raw/` (independent of the root project's `data/processed/`)
- [x] Fresh metadata parsing + WHO-threshold anemia labels (Male<13.0, Female<12.0 g/dL, same both countries) — NOT the country/gender-specific thresholds floated early in the session (India W<12.0/M<14.0, Italy flat<10.5); those were unsourced and would have amplified the India/Italy confound, so the project author explicitly chose WHO thresholds instead
- [x] 4-way (`country + anemic_label`) stratified 70/15/15 patient-level split
- [x] Extract both `palpebral` and `forniceal_palpebral` crops, flattened to a genuinely clean black background (verified: naive `.convert("RGB")` alone is NOT sufficient — see `02_current_status.md`)
- [x] Cross-validated against the root project's independently-computed Phase 0 numbers (217 patients, 126/91 label split, same per-country/split breakdown) — strong signal the fresh reimplementation is correct

## Model architecture & training infrastructure
- [x] Architecture registry: ResNet18, MobileNetV3-Small, EfficientNet-B0 — ImageNet-pretrained, frozen backbone, replaced single-logit head
- [x] Optuna-driven training engine (`trainer_engine.py`) — BCEWithLogitsLoss with `pos_weight`, metrics reported both aggregate and stratified by country
- [x] 6 thin entry-point scripts (3 architectures × 2 tissue types)
- [x] Structural verification (import check + one dry forward pass per architecture/tissue combo, all 6 combos)
- [x] End-to-end local dry-run (1 trial, 1 epoch, `train_resnet18_palpebral` config, run through the real `run_study()` path) — DataLoader, forward/backward, `BCEWithLogitsLoss`, and country-stratified metric computation/persistence all confirmed working with zero runtime errors. Dry-run artifacts (checkpoint/logs) deleted afterward, not committed.
- [x] Actual Optuna training runs (all 6) — **executed on Kaggle, results pulled back 2026-07-19.** All 6 completed the full 12-trial search. Results verified from `study_summary.json` for each + visual inspection of all plot types (not just the two spot-checked during local dry-run verification). See `02_current_status.md` for the full results table and analysis.

## Thesis-grade evaluation upgrade (implemented and verified 2026-07-18)
- [x] Stratified confusion matrix (overall + India + Italy), computed every epoch, plotted as a 3-panel figure for the best trial only
- [x] F1/precision/recall — already computed; now included directly in the per-country JSON summary alongside the new confusion matrix/ROC data
- [x] ROC curve plot (fpr/tpr persisted per epoch, plotted for the best trial's "overall" curve only; per-country fpr/tpr data is also computed and saved in the JSON even though only "overall" gets plotted, since only the confusion matrix was explicitly requested stratified)
- [x] Train vs. validation loss curves over epochs — per-epoch history now persisted via `trial.set_user_attr()` on every trial, plotted for the best trial only
- [x] Plotting strictly scoped to the single best trial per model (`study.best_trial`) — verified no plot is generated for non-winning trials
- [x] Verified via a real end-to-end local dry-run (1 trial, 1 epoch, resnet18/palpebral): all 3 plots (loss curve, ROC curve, confusion matrices) generated correctly, visually inspected, matched the JSON data exactly. Dry-run artifacts deleted after verification, not committed.
- Requested 2026-07-18, implemented and verified same day. See `02_current_status.md` for full detail.

## Kaggle results: comparison across all 6 combinations (2026-07-19)
- [x] Compare palpebral vs. forniceal_palpebral as classification input across all 3 architectures — done, see `02_current_status.md` for the full table
- [x] Identify best overall performer: **EfficientNet-B0 / forniceal_palpebral** (F1=Acc=AUC=0.903, only combo where all three headline metrics agree and clear the rest by a wide margin)
- [x] Identify best confound-handling: **MobileNetV3-Small / forniceal_palpebral** (smallest India/Italy AUC gap, 0.160; the only combo without a blanket recall=1.0, i.e. not just defaulting to "predict anemic")
- [x] **Systematic finding, not one model's quirk:** all 6 independently-tuned models show Italy AUC > India AUC, and 5 of 6 show recall=1.0 for the anemic class — consistent with the `pos_weight` loss term biasing toward "predict anemic," interacting differently with India's anemic-majority vs. Italy's non-anemic-majority val composition. ResNet18/forniceal_palpebral is the clearest case of likely confound exploitation: 78.6% India accuracy paired with a sub-chance 0.450 India AUC.
- [ ] **Not yet decided:** which single (architecture, tissue_type) to carry forward as "the" Phase 4 result, if the thesis needs one — current recommendation is to report both winners (best-overall vs. best-confound-handling) rather than collapsing to one, since they disagree.
- [x] Moved the real Kaggle logs/plots from the doubly-nested `classification/classification/outputs/` into the canonical `classification/outputs/{logs,plots}/` location, verified integrity (all JSONs parse, all 18 PNGs pass `Image.verify()`), and committed as the official experimental record. Checkpoints moved to `classification/outputs/checkpoints/` too but stay local-only (gitignored) per explicit instruction. Also added `*.zip` to `classification/.gitignore` after finding a 125MB leftover results zip with no ignore coverage.
- [ ] Any downstream integration with the root project (e.g. whether a winning classifier eventually gets referenced from the thesis alongside the segmentation results) — out of scope for now, deliberately not decided yet

## Phase 4 expansion (v2): 9 architectures, 18 combos (started 2026-07-28)
Motivated by wanting more than 3 classification models for the thesis. Full rationale, architecture selection reasoning, and verified param counts/resolutions in `02_current_status.md`.

- [x] Reviewed the original 3 models' results *and* actual classifier code (not just the results table) — found a real, previously-unnoticed inconsistency: ResNet18 had zero head-level dropout while MobileNetV3-Small/EfficientNet-B0 each had a hardcoded, untuned p=0.2. This directly motivated standardizing dropout as a searched hyperparameter across all 9 architectures, not just the 6 new ones.
- [x] Selected 3 new CNNs — DenseNet121 (dense connectivity), ConvNeXt-Tiny (transformer-era-redesigned pure CNN), RegNetY-400MF (NAS design-space search) — and 3 new transformers by size tier — Swin-Tiny (lightweight, local windowed attention), ViT-B/16 (medium, canonical global attention), ViT-L/16 (heavy, `IMAGENET1K_SWAG_LINEAR_V1` weights specifically, since the default `IMAGENET1K_V1` weights verified weaker than ViT-B/16's). All 6 confirmed available natively in the installed `torchvision 0.28.0` — zero new dependencies.
- [x] Decided (project author confirmed): retrain all 9 architectures, not just the 6 new ones, under one unified v2 protocol so the final comparison table is strictly apples-to-apples — **18 total (architecture, tissue_type) combos**.
- [x] v2 protocol settings confirmed: `MAX_EPOCHS` 30→100, `EARLY_STOPPING_PATIENCE` 5→7 (raised to tolerate dropout=0.5's added val-loss noise), `dropout_rate` added as a 3rd Optuna-tuned categorical hyperparameter (`{0.2, 0.5}`).
- [x] `ARCHITECTURE_REGISTRY` (`trainer_engine.py`) restructured from `name -> build_fn` to `name -> {build_fn, input_size}`, extended to all 9 architectures. Every builder now takes `dropout_rate` and threads it into a `Dropout -> Linear` head — overwriting the pre-existing fixed-p=0.2 module for MobileNetV3-Small/EfficientNet-B0 (rather than stacking a second dropout), inserting a fresh one for the other 7.
- [x] `dataset.py`'s `get_dataloaders()` made resolution-aware (new `image_size` param, default unchanged at 256) — the 6 CNNs stay at 256 (resolution-flexible via pooling, already proven), the 3 transformers use 224 (position-embedding-locked to their pretrained patch grid).
- [x] Structural verification: all 9 architectures × 2 dropout rates build, forward-pass correctly at their registered resolution (output shape `(B, 1)`), and the sampled `dropout_rate` is confirmed actually present in the model's `Dropout` module (not just silently accepted) — zero errors, exit code 0.
- [x] 18 new entry-point scripts generated in a dedicated `classification/v2_scripts/` directory, isolated from the original `classification/scripts/train_*.py` six (which are now retired and must not be re-run as-is — the shared engine changed underneath them, so re-running one under its old un-suffixed `model_name` would silently overwrite the v1 checkpoint/logs). Import path (`../scripts/trainer_engine.py` relative to `v2_scripts/`) verified working end-to-end, not just visually inspected.
- [x] Local 1-trial/1-epoch dry run, 2026-07-28 — DenseNet121/palpebral (new CNN, 256px) and Swin-T/palpebral (new transformer, 224px), run through the real `run_study()` path with `MAX_EPOCHS` monkey-patched to 1 (temporary, not a code change). Both completed with zero errors; full detail and caveats in `02_current_status.md`.
- [x] Metrics enriched (2026-07-28, project author request for a richer thesis evaluation): `sensitivity` (alias of `recall`), `specificity` (`TN/(TN+FP)`, `None` when undefined rather than silently `0.0`), and `balanced_accuracy` added to `compute_metrics()`/`_safe_metrics()` for all three buckets (overall/India/Italy) uniformly. Every original key (`accuracy`/`precision`/`recall`/`f1`/`auc`/`confusion_matrix`/`roc_curve`) preserved unchanged, so the original 6-combo results stay directly, field-for-field comparable to the new 18-combo results — the explicit "exact same metrics" requirement.
- [x] Plots enriched: new `{model_name}_val_metrics_curve.png` (validation accuracy/F1/sensitivity/specificity over epochs, one figure) and the ROC plot upgraded from single-panel "overall only" to a stratified 3-panel `{model_name}_roc_curves.png` (overall/India/Italy), mirroring the existing confusion-matrix panel style — per-country ROC data was already computed before but never plotted; now it is.
- [x] Verified via a dedicated 1-trial/1-epoch dry run (`resnet18_palpebral`) before touching any of the 9 architecture builders again — confirmed `sensitivity == recall`, `specificity`/`balanced_accuracy` compute correctly (this trial happened to land in the known predict-everyone-anemic collapse mode: `sensitivity=1.0, specificity=0.0, balanced_accuracy=0.5` — a real, useful confirmation that the new metric captures that failure mode correctly, not a synthetic test), and all 4 plot files generate successfully. Dry-run artifacts deleted after verification.
- [x] Decision (project author, 2026-07-28): **skip the Kaggle pilot test**, go straight to the full 18-combo × 12-trial sweep rather than pre-testing timing on 1-2 trials first. Any issues surfacing during real runs get reported and fixed as they come up.
- [x] Kaggle environment set up and verified (2026-07-28) — dataset attached as `Processed-dataset`, real mount path turned out to be `/kaggle/input/datasets/manivafapour33/processed-dataset` (not the assumed `/kaggle/input/processed-dataset`); full working cell recipe and this finding logged in the new `.project_memory/kaggle/01_kaggle_notes.md`. Dataloader sanity check passed for real on Kaggle: `Batch shape: torch.Size([16, 3, 256, 256])`, `Train patients: 151`, `Val patients: 33`.
- [x] Real Kaggle runs split into two batches by weight class (decision recorded in `.project_memory/kaggle/01_kaggle_notes.md`), since the pilot test was skipped and there's no measured per-combo timing to plan a single 18-combo session against: **Batch 1** (queued 2026-07-28, via Kaggle's "Save Version -> Save & Run All") = 14 combos, all 7 light/medium architectures (`regnet_y_400mf`, `mobilenet_v3_small`, `efficientnet_b0`, `resnet18`, `densenet121`, `convnext_tiny`, `swin_t`) × 2 tissue types, cheapest-first. **Batch 2** (not yet run) = `vit_b_16` + `vit_l_16` × 2 tissue types = 4 combos, deferred to a separate session given their much higher per-trial cost.
- [x] **Batch 1 complete (2026-07-29)** — all 14 combos finished on Kaggle, `batch1_results.zip` downloaded and extracted into `classification/outputs/{checkpoints,logs,plots}/`, replacing the old v1 results (which remain recoverable from git history for the 6 combos that were previously committed). All 14 `study_summary.json` files verified as valid, non-corrupt JSON. Best batch-1 result by val F1: EfficientNet-B0/forniceal_palpebral (0.933). Full detail and the "zip carries v1 passenger files" finding in `02_current_status.md`.
- [ ] **Batch 2 in progress** — new dedicated notebook `classification/Kaggle-Notebook/classification-batch2.ipynb` created (2026-07-29) for the 4 remaining combos (`vit_b_16`, `vit_l_16` × 2 tissue types), not yet run. Analysis of batch 1 deliberately paused until batch 2 completes, so all 18 combos get analyzed together.
- [ ] Pull batch-2 results back once done; extend the comparison analysis to all 18 combos — re-run the India/Italy AUC-gap analysis, specifically testing whether global-attention transformers (ViT-B/16, ViT-L/16) show a larger confound gap than local-attention (Swin-T) or no-attention (the 6 CNNs) architectures.
- [ ] Root `CLAUDE.md` update — explicitly out of scope for this module's memory files; root project files are not touched from within `classification/` work (isolation rule, `03_tech_stack_and_rules.md`).
- [x] Literature review on threshold formulation & demographic bias (2026-07-28) — full report `04_literature_review_findings.md`. Found the project's own research-summary docx contained real inaccuracies (caught by cross-checking against the 3 primary-source PDFs). Confirmed empirically that Ramos-Soto et al.'s real, citable country/gender-specific thresholds would *widen* our India/Italy imbalance (52.7pp -> 74.1pp), while Paul et al.'s flat 11.0 g/dL alternative would narrow it (52.7pp -> 30.6pp). CORAL domain adaptation applied specifically to the within-Eyes-Defy India/Italy split (not just cross-dataset, which is what the literature actually tested) flagged as a genuine, untested candidate technique worth prototyping.
- [x] **CLOSED, 2026-07-28: threshold question resolved, staying with WHO (M<13.0, F<12.0).** Deeper follow-up analysis (borderline-density check, real train-split `pos_weight` impact, per-country shortcut-exploitability ceiling — see `02_current_status.md`) found Paul et al.'s flat 11.0 g/dL alternative actually *increases* boundary noise for India specifically (11.0 sits only 0.47 g/dL from India's own mean Hgb) and would push `pos_weight` from 1.397 to 3.314, risking the already-documented pos_weight-driven collapse mode. Project author confirmed: no threshold change, decision final.
